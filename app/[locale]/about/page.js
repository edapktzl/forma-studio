import { copy, photos, pageMetadata } from '../../../lib/content';
import { PageIntro, Picture, Eyebrow, Stats, CallToAction } from '../../../components/ui';
export async function generateMetadata({ params }) { return pageMetadata((await params).locale, 1); }
export default async function About({ params }) {
 const { locale } = await params; const c = copy[locale].about;
 return <><PageIntro {...c}/><div className="shell about-image"><Picture src={photos.team} alt={locale === 'en' ? 'A bright collaborative design studio with shared tables and natural light' : 'Ortak masalar ve doğal ışıkla aydınlık bir tasarım stüdyosu'} eager/><span>FORMA STUDIO / İSTANBUL</span></div><section className="shell story-grid section-pad"><h2>{c.storyTitle}</h2><div>{c.story.map(p => <p key={p}>{p}</p>)}</div></section><Stats locale={locale}/><section className="section-pad values-section"><div className="shell"><Eyebrow>{c.valuesLabel}</Eyebrow><h2>{c.valuesTitle}</h2><div className="values-grid">{c.values.map(([title, text], i) => <article key={title}><span>0{i+1}</span><h3>{title}</h3><p>{text}</p></article>)}</div></div></section><section className="shell section-pad"><Eyebrow>{c.teamLabel}</Eyebrow><h2>{c.teamTitle}</h2><div className="team-grid">{c.team.map(([name, role, bio], i) => <article key={name}><div className={`team-monogram monogram-${i}`} aria-hidden="true"><span>{name.split(' ').map(n=>n[0]).join('')}</span><i>FORMA / 0{i+1}</i></div><h3>{name}</h3><span className="team-role">{role}</span><p>{bio}</p></article>)}</div></section><CallToAction locale={locale}/></>;
}
